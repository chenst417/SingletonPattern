一：单例模式(Singleton Pattern)，

	定义：
		类被加载的时候不进行实例化，被调用的时候才进行实例化。
	特点：
		a:有多种实现方式。  synchronized、 synchronized + volatile(双重检查锁)、静态内部类
		b:需要注意反射和序列化和反序列化的攻击。
		c：延时加载，调用的时候才被初始化
	优点：
		a:
		b：
	缺点：
	  	a：
	应用案例：

	应用场景：
